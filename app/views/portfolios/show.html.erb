<%= image_tag @portfolio_item.main_image %>

<h1><%= @portfolio_item.title %></h1>

<em><%= @portfolio_item.subtitle %></em>

<p><%= @portfolio_item.body %></p>

<h2>Technologies Used:</h2>

<% @portfolio_item.technologies.each do |t| %>
  <p><%= t.name %></p>
<% end %>

<%= link_to "Editar", edit_portfolio_path(@portfolio_item.id) if logged_in?(:site_admin) %>
<%= link_to 'Deletar', portfolio_path(@portfolio_item), method: :delete, data: { confirm: 'Deseja confirmar a exclusão do item?'} if logged_in?(:site_admin)  %>
